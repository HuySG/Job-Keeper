import { NotFoundView } from '@/components/layout/not-found-view';

/** 404 trong một workspace — có đủ thanh điều hướng của workspace đó. */
export default function WorkspaceNotFound() {
  return <NotFoundView />;
}
